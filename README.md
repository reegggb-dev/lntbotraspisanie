# Telegram Schedule Bot for LNTRT

Telegram бот для просмотра расписания занятий ЛНТРТ с сайта http://lntrt.ru/fulltime/daySchedule

## Возможности

- 📚 Выбор группы из списка (46 групп)
- 📅 Просмотр расписания на сегодня и завтра
- 🔄 Удобная навигация с пагинацией
- ⚡ Быстрый доступ к расписанию

## Установка

### 1. Клонирование репозитория

```bash
cd c:/Users/sw_tch0/Downloads/botlnt
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Откройте `.env` и укажите токен вашего бота:

```
BOT_TOKEN=your_bot_token_here
```

**Как получить токен:**
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `.env`

### 4. Запуск бота

```bash
python bot.py
```

## Структура проекта

```
botlnt/
├── bot.py              # Главный файл запуска
├── handlers.py         # Обработчики команд и коллбэков
├── keyboards.py        # Клавиатуры Telegram
├── parser.py           # Парсинг расписания с сайта
├── config.py           # Конфигурация
├── requirements.txt    # Зависимости
├── .env.example        # Пример файла окружения
└── README.md          # Документация
```

## Использование

1. Запустите бота и найдите его в Telegram
2. Отправьте команду `/start`
3. Выберите свою группу из списка
4. Нажмите "Сегодня" или "Завтра"
5. Получите расписание!

## Технологии

- **aiogram 3.14.0** - фреймворк для Telegram ботов
- **BeautifulSoup4** - парсинг HTML
- **requests** - HTTP запросы
- **python-dotenv** - управление переменными окружения

## Группы

Бот поддерживает все группы ЛНТРТ:
- СЭН-25, ПГ-1-25, ПГ-2-25, ЭП-25, М-25
- ИС-1-25, ИС-2-25, ЭКС-1-25, ЭКС-2-25, ЭКС-3-25
- И многие другие (всего 46 групп)

## Лицензия

MIT
